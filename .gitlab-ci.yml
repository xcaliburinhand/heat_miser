before_script:
  - env
stages:
  - deploy
Deploy:
  stage: deploy
  script:
    - rm -rf /var/www/html/*
    - cp -r ${CI_PROJECT_DIR}/www/* /var/www/html/
    - rm -f /opt/heatmiser/*.py
    - cp -r ${CI_PROJECT_DIR}/utility/* /opt/heatmiser/
